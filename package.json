{
  "name": "proctorguard-mvp",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n candidate,admin,author,coordinator,reviewer -c cyan,blue,green,magenta,yellow \"npm run dev --workspace=@proctorguard/candidate\" \"npm run dev --workspace=@proctorguard/admin\" \"npm run dev --workspace=@proctorguard/author\" \"npm run dev --workspace=@proctorguard/coordinator\" \"npm run dev --workspace=@proctorguard/reviewer\"",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "db:migrate": "cd packages/database && npx prisma migrate dev",
    "db:generate": "cd packages/database && npx prisma generate",
    "db:studio": "cd packages/database && npx prisma studio",
    "db:seed": "cd packages/database && npx tsx prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
